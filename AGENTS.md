# AGENTS.md

> This file was generated by [qwykz](https://github.com/MarcZxc1/qwykz).
> It gives AI tools immediate context about this project's stack and conventions.

## Stack

- **Framework** : nextjs
- **Caching**   : none
- **Runtime**   : bun

## Folder Structure

```
├── __tests__/
│   └── example.test.ts
├── app/
│   ├── api/
│   │   ├── contact/
│   │   │   └── route.ts
│   │   └── health/
│   │       └── route.ts
│   ├── about/
│   ├── contact/
│   ├── projects/
│   ├── skills/
│   ├── globals.css
│   ├── layout.tsx
│   └── page.tsx
├── components/
│   ├── site/
│   └── ui/
├── lib/
│   └── portfolio-data.ts
├── public/
│   ├── marc-dagode-photo.png
│   └── marc-dagode-illustrated.png
├── next-env.d.ts
├── next.config.mjs
├── package.json
└── tsconfig.json
```

## Commands

| Command | Purpose |
|---------|---------|
| `bun run dev` | Start development server |
| `bun run typecheck` | TypeScript check |
| `bun run build` | Build for production |
| `bun test` | Run tests |

## How To Add...

- **Add a route**     : Create `app/api/NAME/route.ts` with exported GET/POST handlers
- **Add a test**      : Create `__tests__/NAME.test.ts`, run `bun test`

## API Endpoints

| Method | Path | Description |
|--------|------|-------------|
| GET | `/api/health` | Health check |
| POST | `/api/contact` | Validate and send contact-form email |

## UI Direction

- Read `docs/UI-PREFERENCES.md` before material interface changes.
- Delegate broad visual-direction or preference-discovery work to the
  `portfolio_art_director` custom agent.
- Keep the homepage concise and progressively disclose résumé detail through
  the focused secondary pages.
- Do not add SNUHI unless the user explicitly reverses that instruction.
- Do not start another development server when port 3000 is already in use.

---
*Generated by qwykz — keep this file updated as the project evolves.*
